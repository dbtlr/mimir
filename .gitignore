# Atlas project binding — local, not shared
.atlas.toml

# Claude Code local settings
.claude/settings.local.json

# Local workspace guidance — not shared
CLAUDE.local.md

# Dependencies & build output
node_modules/
dist/
# Compiled standalone binaries (built by the release workflow / `bun build --compile`)
/mimir
/mimir-*

# Local SQLite databases (WAL/SHM sidecars included)
*.db
*.db-wal
*.db-shm

# viteplus cache
.vite-plus/

# Generated embedded-UI manifest (regenerated by check/test/build)
packages/bin/src/http/ui-assets.generated.ts
# vite-plugin-pwa dev output
packages/ui/dev-dist/

# Playwright MCP scratch output (snapshots/screenshots from manual smokes)
.playwright-mcp/
.superpowers/
