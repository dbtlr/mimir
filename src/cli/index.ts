/**
 * The CLI transport — renders the core's intent layer for a human at a
 * terminal (count-led defaults, `isatty`, the table/records/ids/json/jsonl
 * formats). Imports `core` + `contract` only. Phase 2 fills this.
 */
export {};
