### Fixed

- **`update --upstream` on a project or artifact now errors instead of
  silently no-opping** (MMR-284). The flag joins both refuse-lists like every
  sibling scalar flag ("doesn't apply to a project/artifact"), and the
  `update` help descriptor now documents `--upstream` (task only:
  requester-side seed pointer, reference-only). The patch itself shipped in
  v0.15.0 — this closes the gaps around it.
