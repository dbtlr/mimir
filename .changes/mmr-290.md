### Fixed

- **MCP/HTTP envelopes speak token-as-subject on artifact misses** (MMR-290).
  The two transport-local not-found strings (`no artifact with id KEY-aN` in
  the MCP artifact-update tool and `PATCH /api/artifacts/:id`) join the
  token-as-subject family (`KEY-a1 doesn't exist`). A full audit of the
  remaining transport-specific strings (HTTP body validation, MCP tool
  guards) found them already conformant with the
  [output voice guide](docs/output-voice.md). Envelope shape and status
  codes unchanged.
