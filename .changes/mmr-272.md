### Changed

- **Console icons recolored onto the Meridian palette** (MMR-272). The PWA
  install icon and favicon (`mimir.svg`, `mimir-maskable.svg`) still carried
  the pre-Meridian brand colors. The rune-cut "M" stroke now uses the accent
  teal (`--color-accent`, `#4fc4d6`) and the well dot uses the accent
  foreground tint (`--color-accent-foreground`, `#7fd4e2`), matching
  ADR 0019's one-hue accent role. Geometry is unchanged — recolor only.
