{
  "name": "mimir-workspace",
  "version": "0.5.0",
  "private": true,
  "description": "Source of truth for work state — tasks, the work hierarchy, and frozen artifacts. Local-first, agent-driven; the Norn markdown vault is the substrate.",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "mimir": "bun run packages/bin/src/main.ts",
    "assets": "bun packages/bin/scripts/generate-ui-assets.ts",
    "fixtures:vault": "bun packages/bin/scripts/generate-fixture-vault.ts",
    "changelog:compile": "bun packages/bin/scripts/compile-changelog.ts",
    "build:ui": "bun run --cwd packages/ui build",
    "dev:ui": "bun run --cwd packages/ui dev",
    "check": "bun run assets && vp check",
    "test": "bun run assets && bun test packages/bin packages/helpers packages/sandbox",
    "test:pwa": "bun run --cwd packages/ui test:pwa",
    "test:ui": "bun run --cwd packages/ui test",
    "verify": "bun run check && bun run test && bun run test:ui",
    "build": "bun run build:ui && bun run assets && bun build packages/bin/src/main.ts --compile --outfile dist/mimir",
    "sandbox": "bun packages/sandbox/src/main.ts",
    "test:postgres": "bun run sandbox test",
    "test:sandbox": "bun run build && MIMIR_SANDBOX_LIFECYCLE_TEST=1 bun test packages/sandbox/src/workflow.integration.test.ts"
  },
  "dependencies": {
    "vite": "^8.2.0"
  },
  "devDependencies": {
    "@dbtlr/tooling": "0.6.0",
    "@types/bun": "^1.3.14",
    "oxlint": "1.77.0",
    "vite-plus": "^0.2.8"
  },
  "overrides": {
    "@hono/node-server": "2.1.0",
    "body-parser": "2.3.0",
    "brace-expansion": "2.1.4",
    "esbuild": "0.28.1",
    "fast-uri": "3.1.5",
    "hono": "4.13.0",
    "ip-address": "10.4.0"
  },
  "engines": {
    "bun": ">=1.4.0"
  }
}
