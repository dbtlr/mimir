{
  "name": "mimir-workspace",
  "version": "0.5.0",
  "private": true,
  "description": "Source of truth for work state — tasks, the work hierarchy, and frozen artifacts. Local-first, agent-driven; SQLite is the substrate, markdown a projection.",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "mimir": "bun run packages/bin/src/main.ts",
    "assets": "bun packages/bin/scripts/generate-ui-assets.ts",
    "build:ui": "bun run --cwd packages/ui build",
    "dev:ui": "bun run --cwd packages/ui dev",
    "check": "bun run assets && vp check",
    "test": "bun run assets && bun test packages/bin packages/helpers",
    "test:ui": "bun run --cwd packages/ui test",
    "verify": "bun run check && bun run test && bun run test:ui",
    "build": "bun run build:ui && bun run assets && bun build packages/bin/src/main.ts --compile --define 'MIMIR_BUILD_PROFILE=\"production\"' --outfile dist/mimir"
  },
  "dependencies": {
    "vite": "^8.1.3"
  },
  "devDependencies": {
    "@dbtlr/tooling": "0.3.0",
    "@types/bun": "latest",
    "oxlint": "1.72.0",
    "vite-plus": "^0.2.1"
  },
  "overrides": {
    "vite": "^8.1.3"
  }
}
